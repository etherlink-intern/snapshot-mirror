# Snapshot Mirror Environment Configuration
# Copy this file to .env and fill in your values

# Network Storage (NAS) Configuration
NAS_IP=192.168.x.x
SHARE_NAME=your_share_name
STORAGE_USER=your_username
STORAGE_PASSWORD=your_password
# EXPORT_PATH=/path/to/nfs/share  # Required if using NFS

# Sync Configuration
KEEP_COUNT=1
SYNC_INTERVAL=21600
